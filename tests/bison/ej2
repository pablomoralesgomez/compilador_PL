
header{
	int sum(int x, float y, int z);
	int f(int gth);
}

int sum(int x, float y, int z) {
	int res = x + y + z;
	return res;
}

int main() {
	
	int h = 6 + sum(sum(sum(sum(sum(sum(8, 2.5, f(0)), 2.5, f(0)), 2.5, f(0)), 2.5, f(0)), 2.5, f(0)), 2.5, f(0));
	print(h);

}

int f(int gth) {
	return gth * 8;
}

